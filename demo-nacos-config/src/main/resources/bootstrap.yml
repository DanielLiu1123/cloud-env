server:
  port: 7000
spring:
  application:
    name: demo-nacos-config
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        # 在nacos中data-id为 demo-nacos-config.yaml
        name: demo-nacos-config
        file-extension: yaml